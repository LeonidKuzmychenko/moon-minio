server:
  port: 8080

minio:
  url: http://minio:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucket: tiles

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
