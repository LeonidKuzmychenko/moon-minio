server:
  port: 8080

minio:
  url: ${MINIO_URL}
  accessKey: ${MINIO_ACCESS_KEY}
  secretKey: ${MINIO_SECRET_KEY}
  bucket-tile: tile
  bucket-atlas: atlas

spring:
  servlet:
    multipart:
      max-file-size: 150MB
      max-request-size: 150MB
